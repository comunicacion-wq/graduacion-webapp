# Base
PORT=3000
DATABASE_URL=postgres://USER:PASSWORD@localhost:5432/graduacion

# Session
SESSION_SECRET=change_me_super_secret

# App URL (for links sent by WhatsApp)
APP_BASE_URL=http://localhost:3000

# WhatsApp via Twilio (optional). If blank, app runs in SIMULATED mode (logs instead of sending).
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=+14155238886

# Admin WhatsApp number to receive notifications (E.164, e.g. +521XXXXXXXXXX)
ADMIN_WHATSAPP_E164=
