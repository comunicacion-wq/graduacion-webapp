<div class="d-flex justify-content-between align-items-center mb-3">
  <h3>Carreras</h3>
  <a href="/settings" class="btn btn-secondary btn-sm">Volver</a>
</div>

<div class="card mb-4">
  <div class="card-body">
    <form method="POST" action="/settings/careers/new">
      <div class="row">
        <div class="col-md-8">
          <input type="text" name="name" class="form-control" placeholder="Nombre de la carrera" required>
        </div>
        <div class="col-md-4">
          <button class="btn btn-primary w-100">Agregar carrera</button>
        </div>
      </div>
    </form>
  </div>
</div>

<div class="card">
  <div class="card-body">
    <table class="table table-striped">
      <thead>
        <tr>
          <th>ID</th>
          <th>Nombre</th>
          <th>Estatus</th>
          <th>Acciones</th>
        </tr>
      </thead>
      <tbody>

      <% careers.forEach(career => { %>

      <tr>
        <td><%= career.id %></td>
        <td><%= career.name %></td>

        <td>
        <% if(career.active){ %>
        <span class="badge bg-success">Activa</span>
        <% } else { %>
        <span class="badge bg-secondary">Inactiva</span>
        <% } %>
        </td>

        <td>
        <form method="POST" action="/settings/careers/<%= career.id %>/toggle">
        <button class="btn btn-sm btn-outline-primary">
        <%= career.active ? "Desactivar" : "Activar" %>
        </button>
        </form>
        </td>

      </tr>

      <% }) %>

      </tbody>
    </table>
  </div>
</div>
